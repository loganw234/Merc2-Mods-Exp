[repl]
host = 127.0.0.1   ; bind address of the lua bridge (usually localhost)
port = 27050       ; port of the lua bridge

[key]
key = insert       ; key to trigger the dev cheat menu (can be 'insert', or hex code like '0x2D', or decimal like '45')
